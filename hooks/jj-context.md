Use Jujutsu (`jj`) exclusively for version control in this repository. Load the `jj-sensei` `wisdom` skill once, before your first version-control command (status included); from then on it stays in context and routes every later command.
